import { Connection, Repository } from 'typeorm';
import { Artikel } from './artikel.entity';

export const artikelProviders = [
  {
    provide: 'ArtikelRepositoryToken',
    useFactory: (connection: Connection) => connection.getRepository(Artikel),
    inject: ['DbConnectionToken'],
  },
];
